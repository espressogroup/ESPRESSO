import type { ILink } from '@comunica/bus-rdf-resolve-hypermedia-links';
import type { ILinkQueue } from './ILinkQueue';
/**
 * A link queue that wraps a given link queue.
 */
export declare class LinkQueueWrapper implements ILinkQueue {
    private readonly linkQueue;
    constructor(linkQueue: ILinkQueue);
    push(link: ILink, parent: ILink): boolean;
    getSize(): number;
    isEmpty(): boolean;
    pop(): ILink | undefined;
    peek(): ILink | undefined;
}
