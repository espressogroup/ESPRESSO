import { IOidcHandler, IOidcOptions, IRedirector, IStorageUtility, LoginResult } from "@inrupt/solid-client-authn-core";
export default class AuthorizationCodeWithPkceOidcHandler implements IOidcHandler {
    private storageUtility;
    private redirector;
    constructor(storageUtility: IStorageUtility, redirector: IRedirector);
    canHandle(oidcLoginOptions: IOidcOptions): Promise<boolean>;
    handle(oidcLoginOptions: IOidcOptions): Promise<LoginResult>;
}
