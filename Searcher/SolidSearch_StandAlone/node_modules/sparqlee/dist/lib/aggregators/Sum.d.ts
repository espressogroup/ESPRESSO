import type * as RDF from '@rdfjs/types';
import type * as E from '../expressions';
import { BaseAggregator } from './BaseAggregator';
declare type SumState = E.NumericLiteral;
export declare class Sum extends BaseAggregator<SumState> {
    private readonly summer;
    static emptyValue(): RDF.Term;
    init(start: RDF.Term): SumState;
    put(state: SumState, term: RDF.Term): SumState;
    result(state: SumState): RDF.Term;
}
export {};
